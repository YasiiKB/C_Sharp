//using System;
//using System.Collections.Generic;

//namespace CenteredMA
//{
//    class Program
//    {
//        static void Main(string[] args)
//        {

//            var list = new List<double> {1,2,3,4,5,6,7}; // dummy data
//            int windowSize = 4;  // dummy data for user input

//            int numElements = list.Count; 
//            int middleIndex = numElements - windowSize - 1; // -1 because index starts at 0
//            double middelNum = list[middleIndex];  


//            Console.WriteLine("Middle Index: "+ middleIndex);
//            Console.WriteLine("Middle Number: " + middelNum);
//            Console.WriteLine("Length of list: " + numElements);
//            Console.WriteLine("Window Size: " + windowSize);

//            double leftSum = 0.0;
//            var leftIndList = new List<int> {};
//            for (int i = 0; i < middleIndex; i++)
//            {
//                int leftIndex = i;
//                leftIndList.Add(leftIndex);
//                leftSum += list[leftIndex];

//            }
//            Console.WriteLine("Left Sum: "+ leftSum);

//            double rightSum = 0.0;
//            var rightIndList = new List<int> { };
//            for (int i = 1; i < numElements - middleIndex; i++)
//            {
//                int rightIndex = middleIndex + i;
//                rightIndList.Add(rightIndex);
//                rightSum += list[rightIndex];
//            }
//            Console.WriteLine("Right Sum: " + rightSum);

//            double totalSum = leftSum + rightSum + middelNum;
//            int totalLength = leftIndList.Count + rightIndList.Count + 1;
//            double centeredMA = totalSum / totalLength;
//            Console.WriteLine("Centered Moving Average: " + centeredMA);
//        }
//    }
//}

//using System;
//using System.Collections.Generic;

//namespace CenteredMA
//{
//    class Program
//    {
//        static void Main(string[] args)
//        {
//            var list = new List<double> { 1, 2, 3, 4, 5, 6, 7 }; // dummy data
//            int windowSize = 4;  // dummy data for user input

//            // Ensure the window size is even for centered moving average
//            if (windowSize % 2 != 0)
//            {
//                Console.WriteLine("Window size must be even for centered moving average.");
//                return;
//            }

//            int halfWindowSize = windowSize / 2;
//            var centeredMovingAverages = new List<double>();

//            for (int i = halfWindowSize; i < list.Count - halfWindowSize; i++)
//            {
//                double sum = 0.0;
//                for (int j = i - halfWindowSize; j < i + halfWindowSize; j++)
//                {
//                    sum += list[j];
//                }
//                double average = sum / windowSize;
//                centeredMovingAverages.Add(average);
//            }

//            Console.WriteLine("Centered Moving Averages:");
//            foreach (var cma in centeredMovingAverages)
//            {
//                Console.WriteLine(cma);
//            }
//        }
//    }
//}